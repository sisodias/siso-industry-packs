{
  "mode": "candidates",
  "purpose": "Read source-bank snapshots and product-base matches before the broad workflow-derived sweep; verify the first sample-manager search result. This is public-text research only, not application implementation or deployment.",
  "repositories": ["jonasblome/Saempl"]
}
